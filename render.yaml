services:
  - type: web
    name: joblens
    runtime: python
    buildCommand: pip install -r requirements.txt && python seed.py && python scraper.py
    startCommand: gunicorn app:app
    plan: free